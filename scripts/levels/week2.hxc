import funkin.ui.story.Level;
import funkin.save.Save;
import funkin.data.story.level.LevelRegistry;

class Week2Level extends Level
{
  function new()
  {
    super('week2');
  }

  override function isUnlocked():Bool
  {
    super.isUnlocked();

    difficultyList = LevelRegistry.instance.fetchEntry('week1').getDifficulties();

    for (difficulty in difficultyList)
    {
      if (Save.instance.getLevelScore('week1', difficulty) == null)
      {
        continue;
      }
      else
      {
        return true;
      }
    }

    return false;
  }

}