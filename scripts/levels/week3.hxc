import funkin.ui.story.Level;
import funkin.save.Save;
import funkin.data.story.level.LevelRegistry;

class Week3Level extends Level
{
  function new()
  {
    super('week3');
  }

  override function isUnlocked():Bool
  {
    super.isUnlocked();

    difficultyList = LevelRegistry.instance.fetchEntry('week2').getDifficulties();

    for (difficulty in difficultyList)
    {
      if (Save.instance.getLevelScore('week2', difficulty) == null)
      {
        continue;
      }
      else
      {
        return true;
      }
    }

    return false;
  }

}