import funkin.ui.story.Level;
import funkin.save.Save;
import funkin.data.story.level.LevelRegistry;

class Week5Level extends Level
{
  function new()
  {
    super('week5');
  }

  override function isUnlocked():Bool
  {
    super.isUnlocked();

    difficultyList = LevelRegistry.instance.fetchEntry('week4').getDifficulties();

    for (difficulty in difficultyList)
    {
      if (Save.instance.getLevelScore('week4', difficulty) == null)
      {
        continue;
      }
      else
      {
        return true;
      }
    }

    return false;
  }

}