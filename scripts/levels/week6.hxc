import funkin.ui.story.Level;
import funkin.save.Save;
import funkin.data.story.level.LevelRegistry;

class Week6Level extends Level
{
  function new()
  {
    super('week6');
  }

  override function isUnlocked():Bool
  {
    super.isUnlocked();

    difficultyList = LevelRegistry.instance.fetchEntry('week5').getDifficulties();

    for (difficulty in difficultyList)
    {
      if (Save.instance.getLevelScore('week5', difficulty) == null)
      {
        continue;
      }
      else
      {
        return true;
      }
    }

    return false;
  }

}