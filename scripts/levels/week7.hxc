import funkin.ui.story.Level;
import funkin.save.Save;
import funkin.data.story.level.LevelRegistry;

class Week7Level extends Level
{
  function new()
  {
    super('week7');
  }

  override function isUnlocked():Bool
  {
    super.isUnlocked();

    difficultyList = LevelRegistry.instance.fetchEntry('week6').getDifficulties();

    for (difficulty in difficultyList)
    {
      if (Save.instance.getLevelScore('week6', difficulty) == null)
      {
        continue;
      }
      else
      {
        return true;
      }
    }

    return false;
  }

}