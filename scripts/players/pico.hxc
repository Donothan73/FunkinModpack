import funkin.ui.freeplay.charselect.PlayableCharacter;
import funkin.save.Save;
import funkin.data.story.level.LevelRegistry;

class PicoPlayableCharacter extends PlayableCharacter
{
	function new()
	{
		super('pico');
	}

  override function isUnlocked():Bool
  {
    super.isUnlocked();

    difficultyList = LevelRegistry.instance.fetchEntry('weekend1').getDifficulties();

    for (difficulty in difficultyList)
    {
      if (Save.instance.getLevelScore('weekend1', difficulty) == null)
      {
        continue;
      }
      else
      {
        return true;
      }
    }

    return false;
  }
}
